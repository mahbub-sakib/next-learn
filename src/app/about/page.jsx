import React from 'react';

export const metadata = {
    title: "About",
    description: "About page",
    keywords: ["about", "about page"]
};

const page = () => {
    return (
        <div>
            <h6>About Page</h6>
        </div>
    );
};

export default page;